{
  "type": "function",
  "function": {
    "name": "launch_app",
    "description": "Launch an installed Android app by its package name. If you don't know the package name, call get_installed_apps() first.",
    "parameters": {
      "type": "object",
      "properties": {
        "package_name": {
          "type": "string",
          "description": "The Android package name of the app to launch, e.g. 'com.google.android.youtube'"
        }
      },
      "required": ["package_name"]
    }
  }
}
