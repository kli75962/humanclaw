{
  "type": "function",
  "function": {
    "name": "pc_mouse_move",
    "description": "Move the PC mouse cursor to an absolute screen position.",
    "parameters": {
      "type": "object",
      "properties": {
        "x": { "type": "number", "description": "Absolute X coordinate in pixels." },
        "y": { "type": "number", "description": "Absolute Y coordinate in pixels." }
      },
      "required": ["x", "y"]
    }
  }
}
