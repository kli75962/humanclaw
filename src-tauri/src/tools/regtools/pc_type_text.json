{
  "type": "function",
  "function": {
    "name": "pc_type_text",
    "description": "Type text into the currently focused window on the PC.",
    "parameters": {
      "type": "object",
      "properties": {
        "text": { "type": "string", "description": "Text to type." }
      },
      "required": ["text"]
    }
  }
}
