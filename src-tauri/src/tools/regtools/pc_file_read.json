{
  "type": "function",
  "function": {
    "name": "pc_file_read",
    "description": "Read the text contents of a file on the PC filesystem.",
    "parameters": {
      "type": "object",
      "properties": {
        "path": { "type": "string", "description": "Absolute file path." }
      },
      "required": ["path"]
    }
  }
}
