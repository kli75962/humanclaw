{
  "type": "function",
  "function": {
    "name": "pc_run_command",
    "description": "Execute a shell command on the PC and return stdout/stderr. Uses bash on Linux/macOS and cmd.exe on Windows.",
    "parameters": {
      "type": "object",
      "properties": {
        "cmd": { "type": "string", "description": "Shell command to execute." },
        "timeout_secs": { "type": "number", "description": "Max seconds to wait before killing the process (default: 30)." }
      },
      "required": ["cmd"]
    }
  }
}
